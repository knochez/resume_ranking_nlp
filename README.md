# ResumeFit — An NLP Method for Resume Screening

**ResumeFit** scores how well a resume matches a job description (JD) and ranks
candidates for a role. It benchmarks three NLP approaches of increasing
sophistication against a recruiter-assigned ground-truth score.

## Why this matters

Recruiters spend just **6–8 seconds** looking at a resume before deciding
whether to keep or discard it (a widely cited eye-tracking study puts the
average at ~7.4 seconds — [Ladders, 2018](https://www.theladders.com/static/images/basicSite/pdfs/TheLadders-EyeTracking-StudyC2.pdf)).
At that pace, strong candidates are routinely missed and screening is
inconsistent. ResumeFit replaces that snap judgement with a reproducible,
quantitative match score so the first-pass shortlist is fast *and* consistent.

## Approaches compared

| # | Method | Family | Supervised? |
|---|--------|--------|-------------|
| 1 | **TF-IDF + cosine similarity** — sparse lexical baseline. Fast and interpretable, blind to paraphrase. | Lexical | No |
| 2 | **SBERT bi-encoder + cosine** (`all-MiniLM-L6-v2`) — each side encoded independently; captures meaning beyond word overlap. | Dense | No |
| 3 | **Fine-tuned cross-encoder** (`ms-marco-MiniLM-L-6-v2`) — trained directly on the match score with MSE loss; attends jointly across resume and JD. | Pair model | **Yes** |

## Results (held-out test set)

All three approaches are scored on the **same test split** so the comparison
is fair. Numbers are regenerated by running the notebook.

| Approach | Pearson r | Spearman ρ | MAE | NDCG@10 | MRR |
|---|---|---|---|---|---|
| TF-IDF + cosine | 0.16 | 0.11 | 0.34 | 0.77 | 0.67 |
| SBERT + cosine | 0.29 | 0.29 | 0.19 | 0.85 | 0.82 |
| **Cross-encoder (fine-tuned)** | **0.75** | **0.73** | **0.09** | **0.93** | **0.93** |

**Takeaway:** the two unsupervised baselines order candidates reasonably well
but cannot fit the absolute recruiter score. Training a small cross-encoder
directly on the label closes that gap — a ~4× drop in error and a 2.6× jump in
correlation over SBERT.

## Dataset

[Kaggle Resume Dataset](https://www.kaggle.com/datasets/saugataroyarghya/resume-dataset/data)
(`saugataroyarghya/resume-dataset`). Each row is one (resume, JD) pair across
~34 feature columns plus `matched_score` (float, ~0–1) as the ground-truth
label. 

## Repository layout

```
nlp_a3_g92/
├── notebooks/
│   ├── 02_ranking_experiments_colab.ipynb   # PRIMARY — src-backed, run this
│   └── 01_ranking_experiments_colab.ipynb   # self-contained Colab variant (inline logic)
├── src/                       # all logic, unit-tested
│   ├── config.py              # single source of truth for paths & hyperparams
│   ├── data_loader.py         # load CSV, parse list-fields, build composite text
│   ├── preprocessing.py       # clean_text
│   ├── vectorizers/           # tfidf, sentence embeddings (disk-cached)
│   ├── rankers/               # lexical, semantic, cross_encoder
│   ├── evaluation.py          # regression + ranking metrics
│   └── explainability.py      # top shared TF-IDF terms
├── tests/                     # pytest suite covering src/
├── outputs/
│   ├── figures/               # plots from the notebook
│   └── metrics/               # comparison_summary.csv, scored_pairs.csv
├── requirements.txt
└── datasets/resume.csv        # dataset 
```

The notebook is **pure orchestration** — every function lives in `src/` and is
covered by `tests/`.

## Getting started

```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Add the dataset
#    Download the Kaggle CSV and save it as datasets/resume.csv

# 3. Run the tests
pytest tests/ -v

# 4. Run the experiment notebook
jupyter notebook notebooks/02_ranking_experiments_colab.ipynb
```

Section 9 of the notebook fine-tunes the cross-encoder (~25–35 min on a GPU,
slow on CPU). A trained model is reused on subsequent runs if one already
exists at `outputs/models/cross_encoder_resume_jd/`. For a GPU, run it on
Google Colab (T4) or any CUDA machine.

## Evaluation methodology

The task is **dual-framed**, both consuming the same similarity scores:

- **Regression** — predict `matched_score`. Metrics: MAE, RMSE, Pearson r,
  Spearman ρ.
- **Ranking** — group rows by JD; for each JD with ≥2 resumes, rank them.
  Metrics: NDCG@10, Precision@10, MRR. resumes with `matched_score` ≥ the
  dataset median are treated as relevant for the binary metrics.

## Limitations

- **Sequence truncation** — the cross-encoder truncates at 384 tokens; long
  resumes lose late-section content.
- **JD leakage** — the i.i.d. split lets JDs recur across train/test; a
  by-JD split would give a stricter, more honest number.
- **Inference cost** — the cross-encoder is O(N·M); production would use SBERT
  to shortlist, then the cross-encoder to rerank the top-K.

---

*Academic project — Year 4 Natural Language Processing coursework.*
